{
  "name": "pirates-tools",
  "version": "1.0.0",
  "private": true,
  "description": "Pirates Tools — PWA e-commerce with serverless API (Vercel + Firebase + Revolut)",
  "scripts": {
    "test": "node scripts/ci.js",
    "dev": "npx vercel dev --listen 3000"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  },
  "engines": {
    "node": "22.x"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "css-tree": "^3.2.1",
    "escodegen": "^2.1.0",
    "esprima": "^4.0.1",
    "estraverse": "^5.3.0"
  }
}
